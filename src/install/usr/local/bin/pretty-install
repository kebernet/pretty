#!/bin/bash
chmod +x /etc/init.d/pretty
chmod +x /usr/local/bin/pretty
chmod +x /usr/local/bin/pretty-import-key
ln -s /etc/init.d/pretty /etc/rc3.d/S98pretty
chmod -R a+r /etc/pretty
chmod a-r /etc/pretty/settings.properties